// Place any employee services like formatting and cache (redis) but not a direct fetching!
// The direct fetching is done by using the model!
import { Employee } from "../models";

export default class EmployeeService {

}